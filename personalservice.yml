feign:
  hystrix:
    enabled: true       
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 10000
#ribbon的超时时间
ribbon:
  ReadTimeout: 7000
  ConnectTimeout: 5000
#OSS bucket  
bucketName: tymp-bucket
#小程序 pagePath
pagePath: card/person
